apiVersion: apps/v1
kind: Deployment
metadata:
  name: foldx-worker-image
  namespace: foldx-prod
  labels:
    app.kubernetes.io/name: foldx
    app.kubernetes.io/component: worker-image
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: foldx
      app.kubernetes.io/component: worker-image
  template:
    metadata:
      labels:
        app.kubernetes.io/name: foldx
        app.kubernetes.io/component: worker-image
    spec:
      containers:
        - name: worker-image
          image: foldx:latest
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "npm run start:worker"]
          env:
            - name: WORKER_QUEUES
              value: "image"
          envFrom:
            - configMapRef:
                name: foldx-app-config
            - secretRef:
                name: foldx-app-secret
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "3"
              memory: "4Gi"
      terminationGracePeriodSeconds: 30
