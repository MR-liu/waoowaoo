apiVersion: v1
kind: Service
metadata:
  name: waoowaoo-web
  namespace: waoowaoo-prod
  labels:
    app.kubernetes.io/name: waoowaoo
    app.kubernetes.io/component: web
spec:
  selector:
    app.kubernetes.io/name: waoowaoo
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: 3000
